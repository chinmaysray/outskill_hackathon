import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Multimodal AI Design Analysis Suite',
  description: 'Advanced AI-powered design analysis with specialized agents for comprehensive insights',
  keywords: ['AI', 'Design Analysis', 'Machine Learning', 'Computer Vision'],
  authors: [{ name: 'AI Design Analysis Team' }],
  robots: 'index, follow',
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
